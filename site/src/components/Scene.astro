---
// Basis-Wrapper für jede Szene: 100vh, Kapitel-Akzent, Kicker + Szenen-Nummer via Slot.
interface Props {
  chapter?: string;
  accent?: string;
  kicker?: string;
  index?: number;
  total?: number;
  id?: string;
}
const { chapter, accent, kicker, index, total, id } = Astro.props;
---

<section class:list={['scene']} data-accent={accent} id={id} data-chapter={chapter}>
  {kicker && <p class="kicker">{kicker}</p>}
  <slot />
  {
    index !== undefined && total !== undefined && (
      <p class="scene-num" style="margin-top: 2rem;" aria-hidden="true">
        {String(index + 1).padStart(2, '0')} / {String(total).padStart(2, '0')}
      </p>
    )
  }
</section>